import {
  EntityType,
  GraphQLFunctionType,
  GraphRefInputType,
  ImplementationType,
  MaanaErrorCode,
  NodeType,
  ServiceType,
} from './constants';
import { GraphqlOperationType, Scalars, Service } from './schema';

export type Maybe<T> = T | null | undefined;
export type InputMaybe<T> = Maybe<T>;
